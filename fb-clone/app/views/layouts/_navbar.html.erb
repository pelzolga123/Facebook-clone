
    <nav class = 'navbar navbar-dark bg-primary'>
      <a class="navbar-brand" href="#">Facebook</a>

      <ul class='navbar-nav mr-auto'>
        <% if user_signed_in? %>
          <li class="nav-item active"><%= link_to 'Users', :users, :class=> "nav-link" %></li> 
          <li class="nav-item active"><%= link_to 'My profile',profile_path(current_user), :class=> "nav-link" %></li> 
          <li class="nav-item active"><%= link_to 'Posts',:posts, :class=> "nav-link" %></li> 
          <li class="nav-item active"><%= link_to 'Sign out', destroy_user_session_path, :method => 'delete', :class=> "nav-link" %></li> 
          <% if !@expected.nil? %>
          <div class='dropdown'>
              <li class="nav-item active"> Friends requests </li> 
          <div class="dropdown-content"> 
          <% @expected.each do |friend| %>
            <li>
                <span role="menuitem" tabindex="-1" ><%= friend.user.first_name %></span>
                <span class="btn btn-success btn-xs "><%= link_to 'accept', friendship_path(id: friend.id), method: :patch %></span>

              </li>
            <% end %>
            </div>
          </div>   
          <% end %>
        <% else %>
          <li class="nav-item active"><%= link_to 'Log in' , new_user_session_path, :class=> "nav-link" %></li> 
          <li class="nav-item active"><%= link_to 'Sign up' , new_registration_path(resource_name), :class=> "nav-link" %></li>
        <% end %>
      </ul>
    </nav>